# 前端 CRUD + 导入导出提示词模板（极简填空版）

```text
使用 /Users/shibin/.codex/skills/frontend-crud-exec-skill/SKILL.md 执行。

模块名：{中文名}
资源名：{英文名}
交付标准：{最低交付/标准交付/严格交付}
字段：{直接贴字段清单，含类型/约束说明更好}
查询字段：{用于查询区的字段}
列表字段：{用于表格展示的字段}
导入模板列：{导入xlsx列名；默认不含id和关联id}
能力：{
导入=是/否,
导出=是/否,
步骤=是/否,
明细=是/否,
统计=是/否,
状态标签=是/否,
标签页=是/否,
地图选址=是/否,
图片上传=是/否,
页面点击自动化=是/否
}
特殊：{可空；例如“查看页仅上层分组、图片字段=files、导入附加参数=planId=xxx”}
联调访问：{
需要token=是/否,
访问URL格式=/#/{resource}?token=...&tenantId=...&userId=...,
token获取方式=登录接口/cookie/其他
}
报告分层：{
功能可用性=是/否,
工程门禁(lint/build/环境告警)=是/否
}

测试验收：{
最低交付=列表查询+新增+编辑+删除+查看+导入/导出可用,
标准交付=最低交付+关键字段校验+状态标签/统计联动,
严格交付=标准交付+关键路径自动化+测试报告落docs/reports,
页面自动化关键路径默认=打开+查询+编辑+删除确认+导入弹窗+导出弹窗
}

测试同步生成与门禁：{
生代码=生用例=是,
字段口径自动化=必填/长度/枚举/格式/唯一性/关联回填,
覆盖率阈值(前端)=line>=60% branch>=50%,
核心模块覆盖率阈值=line>=80%,
门禁时机=分支合并前
}
```
